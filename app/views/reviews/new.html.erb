<div class="container">
  <div class="row">
    <div class="col-6 offset-3">
      <h1>Add a review</h1>

      <%= simple_form_for [@restaurant, @review] do |f| %>
        <%= f.input :content %>
        <%= f.submit %>

      <% end %>
    </div>
  </div>
</div>

